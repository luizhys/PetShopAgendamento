@model IEnumerable<PetShopAgendamento.Models.Pet>

@{
    ViewData["Title"] = "Pets";
}

<div class="d-flex flex-column flex-sm-row justify-content-between align-items-sm-center mb-3 gap-2">
    <div>
        <h1 class="fw-semibold mb-1">Pets</h1>
        <p class="text-muted">Gerencie os animais cadastrados.</p>
    </div>
    <a asp-action="Create" class="btn btn-teal">
        <i class="fas fa-plus"></i> Adicionar Pet
    </a>
</div>

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        @TempData["ErrorMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}
@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        @TempData["SuccessMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

<div class="table-responsive">
    <table class="table table-hover align-middle">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(model => model.Nome)</th>
                <th class="d-none d-lg-table-cell">@Html.DisplayNameFor(model => model.Cliente)</th>   <!-- oculta em telas < 992px -->
                <th>@Html.DisplayNameFor(model => model.Porte)</th>
                <th class="d-none d-md-table-cell">@Html.DisplayNameFor(model => model.Peso)</th>    <!-- oculta em telas < 768px -->
                <th class="d-none d-md-table-cell">@Html.DisplayNameFor(model => model.Raca)</th> <!-- oculta em telas < 768px -->
                <th class="text-end">Ações</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Nome)</td>
                    <td class="d-none d-lg-table-cell">@Html.DisplayFor(modelItem => item.Cliente!.Nome)</td>
                    <td>@Html.DisplayFor(modelItem => item.Porte)</td>
                    <td class="d-none d-md-table-cell">@Html.DisplayFor(modelItem => item.Peso) kg</td>
                    <td class="d-none d-md-table-cell">@(string.IsNullOrEmpty(item.Raca) ? "—" : item.Raca)</td>
                    <td class="text-end">
                        <div class="d-flex gap-2 justify-content-end flex-wrap">
                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-teal-sm" title="Editar">
                                <i class="fas fa-pen"></i>
                            </a>
                            <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-teal-sm" title="Detalhes">
                                <i class="fas fa-info-circle"></i>
                            </a>
                            <button type="button" class="btn btn-teal-sm"
                                    data-bs-toggle="modal"
                                    data-bs-target="#genericDeleteModal"
                                    data-id="@item.Id"
                                    data-nome="@item.Nome"
                                    data-action-url="@Url.Action("Delete", "Pets")"
                                    title="Excluir">
                                <i class="fas fa-trash"></i>
                            </button>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@await Html.PartialAsync("_DeleteModal")