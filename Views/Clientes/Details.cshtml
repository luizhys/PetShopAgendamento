@model PetShopAgendamento.Models.Cliente
@using PetShopAgendamento.Utils

@{
    ViewData["Title"] = "Detalhes do Cliente";
}

<div class="text-center mb-4">
    <h1 class="fw-semibold">Detalhes do Cliente</h1>
    <p class="text-muted">Visualize as informações cadastrais do cliente.</p>
</div>

<div class="d-flex justify-content-center">
    <div class="col-12 col-md-6">
        <div class="card p-3 p-md-4 shadow-sm mb-4">
            <dl class="row mb-0">
                <dt class="col-12 col-md-4 fw-semibold">@Html.DisplayNameFor(model => model.Nome)</dt>
                <dd class="col-12 col-md-8 mb-3 pb-2 border-bottom">@Model.Nome</dd>

                <dt class="col-12 col-md-4 fw-semibold">@Html.DisplayNameFor(model => model.Telefone)</dt>
                <dd class="col-12 col-md-8 mb-3 pb-2 border-bottom">
                    @FormatadorTelefone.Formatar(Model.Telefone)
                </dd>

                <dt class="col-12 col-md-4 fw-semibold">@Html.DisplayNameFor(model => model.Email)</dt>
                <dd class="col-12 col-md-8 mb-3 pb-2 border-bottom">
                    @(string.IsNullOrEmpty(Model.Email) ? "—" : Model.Email)
                </dd>

                <dt class="col-12 col-md-4 fw-semibold">@Html.DisplayNameFor(model => model.Endereco)</dt>
                <dd class="col-12 col-md-8 mb-3 pb-2 border-bottom">@Model.Endereco</dd>
            </dl>

            <div class="d-flex flex-column flex-sm-row gap-2 justify-content-end mt-3">
                <a asp-action="Index" class="btn btn-teal">
                    <i class="fas fa-arrow-left"></i> Voltar
                </a>
                <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-teal">
                    <i class="fas fa-edit"></i> Editar
                </a>
            </div>
        </div>
    </div>
</div>